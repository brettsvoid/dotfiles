source "$HOME/.config/colors.sh"

options=(
	style=round
	width=8.0
	#hidpi=off
	active_color=$COLOR_ACCENT_BRIGHT
	inactive_color=$COLOR_BACKGROUND
	#background_color=$COLOR_TRANSPARENT
)

borders "${options[@]}"
