name: sample
root: ~/projects/sample  # Change to your project path

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim .
  - server:
      panes:
        - # Your dev server command, e.g.: npm run dev
  - shell:
      panes:
        -  # Empty shell for git, tests, etc.
